import * as THREE from 'three';

export const canvasWidth = 240;
export const canvasHeight = 240;

export const graphicOffsetX = canvasWidth / 2;
export const graphicOffsetY = canvasHeight / 4;

export const colors = ['#F7A541', '#F45D4C', '#FA2E59', '#4783c3', '#9c6cb7'];

export const cameraLookAt = new THREE.Vector3(0, 0, 0);
